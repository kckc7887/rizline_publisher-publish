name: 构建与发布曲库

on:
  schedule:
    # UTC 12:00 = Asia/Shanghai 20:00, once per day.
    - cron: '0 12 * * *'
  workflow_dispatch:
    inputs:
      execute:
        description: 实际上传至 S3 并切换 current（不勾选则只构建产物）
        type: boolean
        default: false
      upload_workers:
        description: 资源上传与校验并发数
        type: choice
        default: '16'
        options:
          - '4'
          - '8'
          - '12'
          - '16'
          - '1'
      parse_workers:
        description: 资源解析、构建与本地校验并发数
        type: choice
        default: '4'
        options:
          - '4'
          - '8'
          - '2'
          - '1'

permissions:
  contents: read

# Serialize complete publication runs so their current pointers cannot race.
concurrency:
  group: rizline-resource-publication
  cancel-in-progress: false

env:
  PYTHONUTF8: '1'
  PYTHONUNBUFFERED: '1'

jobs:
  build:
    name: 导入、校验与构建
    runs-on: ubuntu-latest
    timeout-minutes: 90
    outputs:
      release-artifact-id: ${{ steps.release.outputs.artifact-id }}
    steps:
      - name: 检查发布分支
        if: (github.event_name == 'schedule' || inputs.execute) && github.ref != 'refs/heads/main'
        run: |
          echo '::error::实际发布仅允许从 main 运行；其它分支请取消实际上传。'
          exit 1
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          persist-credentials: false
      - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6
        with:
          python-version: '3.13'
          cache: pip
      - name: 恢复上游资源缓存
        uses: actions/cache@caa296126883cff596d87d8935842f9db880ef25 # v5
        with:
          path: .cache/http
          key: rizline-http-v1-${{ runner.os }}-${{ github.run_id }}-${{ github.run_attempt }}
          restore-keys: |
            rizline-http-v1-${{ runner.os }}-
      - name: 安装依赖
        run: python -m pip install -r requirements.txt
      - name: 运行发布事务测试
        run: python -m unittest discover -s tests -v
      - name: 导入当前官方资源
        env:
          PARSE_WORKERS: ${{ inputs.parse_workers || '4' }}
        run: python -m rizline_publisher import --transport urllib --workers "$PARSE_WORKERS"
      - name: 校验并构建不可变版本
        env:
          PARSE_WORKERS: ${{ inputs.parse_workers || '4' }}
        run: |
          python -m rizline_publisher validate
          python -m rizline_publisher build --workers "$PARSE_WORKERS"
          python -m rizline_publisher validate --release --workers "$PARSE_WORKERS"
      - name: 生成上传计划
        env:
          UPLOAD_WORKERS: ${{ inputs.upload_workers || '16' }}
        run: python -m rizline_publisher publish --workers "$UPLOAD_WORKERS" > work/upload-plan.json
      - name: 保存可发布资源
        id: release
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: rizline-release-${{ github.run_id }}-${{ github.run_attempt }}
          path: dist/
          if-no-files-found: error
          compression-level: 0
          retention-days: 90
      - name: 保存导入报告与补充表
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: rizline-reports-${{ github.run_id }}-${{ github.run_attempt }}
          path: |
            work/import-report.json
            work/import-failures.json
            work/supplement-template.json
            work/upload-plan.json
          if-no-files-found: ignore
          retention-days: 90
      - name: 写入构建摘要
        env:
          EXECUTE: ${{ github.event_name == 'schedule' || inputs.execute }}
        run: |
          python - <<'PY'
          import json
          import os
          from pathlib import Path

          current = json.loads(Path('dist/rizline/current.json').read_text())
          report = json.loads(Path('work/import-report.json').read_text())
          with open(os.environ['GITHUB_STEP_SUMMARY'], 'a', encoding='utf-8') as summary:
              summary.write('## 曲库构建完成\n\n')
              summary.write(f"版本：`{current['resourceVersion']}`\n\n")
              summary.write('```json\n' + json.dumps(report['summary'], ensure_ascii=False, indent=2) + '\n```\n\n')
              summary.write('资源、报告和补充表可在本次运行的 Artifacts 下载。\n\n')
              summary.write('等待发布任务执行；请以发布任务结果为准。\n' if os.environ['EXECUTE'] == 'true' else '本次仅构建与校验，没有上传到 S3。\n')
          PY

  publish:
    name: 验证产物与按需发布
    needs: build
    runs-on: ubuntu-latest
    # Delta CopyObject stays in-region; --delta-only refuses a full overseas PUT.
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          persist-credentials: false
      - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6
        with:
          python-version: '3.13'
          cache: pip
      - name: 安装依赖
        run: python -m pip install -r requirements.txt
      - name: 下载本次构建的资源
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          artifact-ids: ${{ needs.build.outputs.release-artifact-id }}
          merge-multiple: true
          path: dist/
      - name: 验证下载产物的完整性
        run: python -m rizline_publisher validate --release
      - name: 并行上传、切换版本并清理旧资源
        if: (github.event_name == 'schedule' || inputs.execute) && github.ref == 'refs/heads/main'
        shell: bash
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_EC2_METADATA_DISABLED: 'true'
          UPLOAD_WORKERS: ${{ inputs.upload_workers || '16' }}
        run: |
          set -o pipefail
          python - <<'PY'
          import os
          import sys

          required = ('AWS_ACCESS_KEY_ID', 'AWS_SECRET_ACCESS_KEY')
          missing = [name for name in required if not os.environ.get(name, '').strip()]
          if missing:
              print('::error::缺少 GitHub Actions 配置：' + ', '.join(missing))
              sys.exit(1)
          PY
          python -m rizline_publisher publish --execute --delta-only --workers "$UPLOAD_WORKERS" --report work/publication-report.json --publication-output work/publication-release | tee publish-result.json
      - name: 保存实际发布版本
        if: always() && (github.event_name == 'schedule' || inputs.execute) && github.ref == 'refs/heads/main'
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: rizline-publication-${{ github.run_id }}-${{ github.run_attempt }}
          path: work/publication-release/
          if-no-files-found: ignore
          compression-level: 0
          retention-days: 90
      - name: 保存发布报告与清理重试记录
        if: always() && (github.event_name == 'schedule' || inputs.execute) && github.ref == 'refs/heads/main'
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: rizline-publication-report-${{ github.run_id }}-${{ github.run_attempt }}
          path: |
            work/publication-report.json
            work/cleanup-receipts/
          if-no-files-found: ignore
          retention-days: 90
      - name: 写入发布摘要
        if: always() && (github.event_name == 'schedule' || inputs.execute) && github.ref == 'refs/heads/main'
        run: |
          python - <<'PY'
          import json
          import os
          from pathlib import Path

          path = Path('work/publication-report.json')
          with open(os.environ['GITHUB_STEP_SUMMARY'], 'a', encoding='utf-8') as summary:
              summary.write('## 发布结果\n\n')
              if not path.exists():
                  summary.write('发布未生成报告，请检查前置步骤的错误。\n')
              else:
                  result = json.loads(path.read_text())['publication']
                  summary.write({'published': '发布完成：新版已验证并切换，原 current 版本的快照资源已清理。', 'unchanged': '清单一致：跳过所有资源上传，不下载封面做比较。', 'failed': '发布失败：以阶段、currentSwitched 和剩余删除键为准；可下载报告与重试记录。'}.get(result['status'], result['status']) + '\n\n')
                  summary.write('```json\n' + json.dumps(result, ensure_ascii=False, indent=2) + '\n```\n\n')
              summary.write('[公共版本指针](https://rranker-rizline-data.cn-nb1.rains3.com/rizline/current.json)\n')
          PY
